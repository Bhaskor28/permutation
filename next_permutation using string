void s21(){
	string s;
	cin>>s;
	ll l=s.size();
	sort(s.begin(),s.end());
	vector<string>ans;
	do{

		ans.pb(s);


	}
	while(next_permutation(s.begin(),s.end()));
	cout<<ans.size()<<endl;
	for(auto x:ans){
		cout<<x<<endl;
	}

	}

time complexity O(n*n!)
